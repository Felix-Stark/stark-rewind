export interface Game {
      activity: string;
      players: Array<string>;
      minutesPlayed: string;
      winner: string;
      secondPlace: string;
      thirdPlace: string;
      date: string;
      gameId: number
}

